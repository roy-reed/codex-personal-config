# 贡献指南

请把本仓库当作公开的、可迁移配置模板仓库，而不是个人配置备份。

- 新增内容必须说明用途、适用平台和失败时的回滚方式。
- 使用环境变量、相对路径和平台检测，避免写死用户目录、盘符和本机端口。
- 不提交真实令牌、Cookie、私钥、证书、浏览器资料或个人/工作区原文。
- 提交前执行 `git diff --check`，并在变更涉及的平台上运行最小验证。
- 配置格式变更要同步更新 README 或 `docs/` 中的迁移说明。\n
